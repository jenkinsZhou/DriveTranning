<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tourcoo.training">
<!--    读写权限-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <!--摄像头相关权限-->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <!--8.0开启安装应用权限-->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name="com.tourcoo.training.core.app.MyApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">
        <activity android:name="com.tourcoo.training.ui.MainTabActivity"
            android:launchMode="singleTop"
            >
        </activity>
        <activity android:name="com.tourcoo.training.ui.MainActivity"/>
        <activity android:name="com.tourcoo.training.core.base.activity.QQTitleActivity"/>
        <activity android:name=".ui.study.FreeStudyActivity" />
        <activity android:name=".ui.SplashActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>
        <activity android:name=".ui.face.FaceRecognitionActivity" />
        <activity android:name=".ui.face.DialogFaceRecognitionActivity"
            />
        <activity android:name=".ui.account.IndustrialRegisterActivity" />
        <activity android:name=".ui.account.DriverRegisterActivity" />
        <activity android:name=".ui.account.LoginActivity" />
    </application>

</manifest>
